<header class="pb-3">
  <h1><%= @player.name %></h1>
  <small><%= t '.synchronized' %> <%= time_ago @player.synchronized_at %></small>
</header>

<h3><%= t('.rivals') %></h3>
<%= render 'rivals' %>

<h3><%= t('.battles') %></h3>
<%= render 'battles' %>
