<% [ :favorites, :victims, :tormentors ].each do |kind| %>
    <%= render "rivals/rival_table", items: rivals.public_send(kind), title: Rivals.human_attribute_name(kind), matches_filter_attributes: %>
<% end %>
