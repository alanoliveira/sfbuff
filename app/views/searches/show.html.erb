<div class="mb-3">
  <%= render "search_form", form_options: { data: { turbo_frame: "fighter_search", turbo_action: "advance" } } %>
</div>

<% if @query %>
  <div class="card card-body table-responsive">
    <%= turbo_frame_tag "fighter_search" do %>
      <%= search_request_form(@query) %>
    <% end %>
  </div>
<% end %>
