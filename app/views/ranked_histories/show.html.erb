<div class="card card-body mb-3">
  <%= render "form", ranked_history: @ranked_history %>
</div>

<div class="card card-body">
  <%= render_chart(render partial: "chart_data", formats: :json, locals: { ranked_history: @ranked_history }) %>
</div>
