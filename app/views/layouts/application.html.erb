<!DOCTYPE html>
<html lang="<%= I18n.locale.name %>" data-bs-theme="dark" data-controller="timezone">
  <head>
    <title><%= content_for(:title) || "SFBUFF" %></title>
    <meta name="description" content="A statistics website for SF6 matches">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Sfbuff">
    <meta name="mobile-web-app-capable" content="yes">
    <% if @unindexable %>
      <meta name="robots" content="noindex">
    <% end %>
    <% if @ads_elegibility %>
      <%== AppSetting.google_ads_tag if AppSetting.google_ads_tag.present? %>
    <% end %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="twitter:title" content="SFBUFF">
    <meta name="twitter:description" content="A statistics website for SF6 matches">
    <meta name="twitter:image" content="<%= url_to_asset "/icon.png" %>">
    <meta name="twitter:card" content="summary">
    <% AppSetting.extra_meta_tags.to_h.each do |name, content| %>
      <%= content_tag :meta, nil, name:, content: %>
    <% end %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <% if Rails.env.production? %>
      <!-- workaround to use import maps in old browsers -->
      <script async src="https://ga.jspm.io/npm:es-module-shims@2.6.2/dist/es-module-shims.js"></script>
    <% end %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <% if ENV["SFBUFF_USE_LOCAL_ASSETS"] %>
      <%= stylesheet_link_tag "/assets/bootstrap/dist/css/bootstrap", skip_pipeline: true %>
      <%= stylesheet_link_tag "/assets/bootstrap-icons/font/bootstrap-icons", skip_pipeline: true %>
    <% else %>
      <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.css" %>
      <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.css" %>
    <% end %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="vh-100 d-flex flex-column">
    <%= render "layouts/application/nav" %>

    <main id="main" class="container my-3 flex-fill">
      <% AppSetting.general_alerts.to_a.each do |alert_item| %>
        <%= general_alert_item alert_item %>
      <% end %>

      <%= content_for?(:main) ? yield(:main) : yield %>
    </main>

    <%= modal_turbo_frame_tag %>
    <%= toasts_container %>

    <%= render "layouts/application/footer" %>
  </body>
</html>
