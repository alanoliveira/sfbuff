<!DOCTYPE html>
<html>
  <head>
    <%= head_title %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-prefetch" content="false">
    <meta name="description" content="SFBUFF is a statistics website for SF6 matches.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body data-bs-theme="dark" class="min-vh-100 d-flex flex-column">
    <header class="bg-body-tertiary" style="min-height: 3.4em">
      <%= render 'layouts/nav' unless current_page? root_path  %>
    </header>
    <main class="container py-3 mb-auto">
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
    <footer class="footer py-3 bg-body-tertiary">
      <div class="container-fluid d-flex align-items-center justify-content-between">
        <div>
          <div class="input-group input-group-sm">
            <label class="input-group-text" for="language-select">
              <i class="bi bi-globe"></i>
            </label>
            <%= language_select class: 'form-select form-select-sm' %>
          </div>
        </div>
        <% Rails.configuration.sfbuff[:repo_url].try do |url| %>
          <div class="text-end">
            <a target="_blank" class="link-light link-underline link-underline-opacity-0 link-underline-opacity-75-hover" href="<%= url %>">
              <i class="bi bi-github"></i>
              github
            </a>
          </div>
        <% end %>
      </div>
    </footer>
  </body>
</html>
