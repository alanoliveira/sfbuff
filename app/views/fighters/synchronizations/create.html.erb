<%= fighter_synchronization_turbo_frame_tag do %>
  <%= content_tag :div, data: { controller: "turbo-stream-loader", turbo_stream_loader_url_value: fighter_synchronization_path } do %>
    <div class="position-absolute start-50" id="<%= dom_id @synchronization %>">
      <div class="badge rounded-pill text-bg-light opacity-50">
        <div class="spinner-border" role="status">
          <span class="visually-hidden">Loading...</span>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
